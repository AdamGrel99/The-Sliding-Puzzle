const size = document.querySelectorAll('.puzzle-number input');
const resetButton = document.getElementsByClassName('button-reset');
const changePictureButton = document.getElementById('change-image-button');
const helperImage = document.getElementById('template');